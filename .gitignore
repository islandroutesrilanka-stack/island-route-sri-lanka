node_modules
.next
out
.env*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Filesystem artefacts — created when a file is deleted while still open
.fuse_hidden*
Thumbs.db

# Bundle analyzer output
.next/analyze
analyze

# Generated QA output (scripts/qa-site.mjs)
qa-results.json
qa-results.md

# Source photographs (working originals, 2-15MB each).
# Optimised, web-ready derivatives live in public/photography/ and ARE committed.
photo-candidates/
